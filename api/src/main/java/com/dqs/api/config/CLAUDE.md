# Config

App configuration: CORS, Jackson serialization, OpenAPI.

## Always ask before changing
- CORS misconfiguration blocks the frontend immediately
- Jackson config changes affect how all JSON is serialized/deserialized across the entire API
